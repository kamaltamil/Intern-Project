const User=require('../../models/user');
describe('User model',()=>{test('has expected schema',()=>{expect(User.schema.path('name').options.required[1]).toBe('Name is required');expect(User.schema.path('username').options.lowercase).toBe(true);expect(User.schema.path('email').options.unique).toBe(true);expect(User.schema.path('role').options.ref).toBe('Role');expect(User.schema.path('isActive').defaultValue).toBe(true);});test('validates required fields',async()=>{const e=await new User({}).validate().catch(x=>x);expect(e.errors.name).toBeDefined();expect(e.errors.username).toBeDefined();expect(e.errors.email).toBeDefined();expect(e.errors.password).toBeDefined();expect(e.errors.role).toBeDefined();});});
